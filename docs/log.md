# 작업 로그 (log.md)

## [2026-06-23] init | 위키 완전 초기화(raw 포함) — 모든 페이지·원본·기록 삭제, 백지 상태로 재시작
## [2026-06-23] ingest | 유튜브 재생목록 PLUGinkN1Rwv4KGXiVEmSBzglfAoTxyXd3 영상 25편 일괄 흡수 — raw/youtube-*.md 25개 저장(자막 24 ko/1 en) → sources 25개 + concepts 17개(harness-engineering, prompt/context/loop-engineering, agentic-coding, developer-role-change, claude-md, skills, subagents-agent-teams, hooks, mcp, dynamic-workflow, multi-model-workflow, verification-automation, context-decay, on-device-ai, llm-basics) + entities 12개(claude-code, anthropic, openai, codex, cursor, vercel, skills-sh, google-gemma, andrej-karpathy, boris-cherny, mitchell-hashimoto, peter-steinberger) 생성, overview + index 전면 작성. 모순①(claude-md 삭제 vs 3배)·긴장②(하네스 다이어트)·긴장③(멀티모델 vs 바닐라) 명시. 자막 오인식 교정: "André Capaci/카파시"=Andrej Karpathy 통합. 링크 무결성 검증 완료(고아 0).
## [2026-06-23] lint | 정합성 25/25·깨진링크 0·본문고아 1건(#9 JzB_GI7SS6g) 발견 → developer-role-change·verification-automation에 엮어 해소, sources 갱신
## [2026-06-23] analysis | 분석 페이지 3종 생성 — analysis/ai-coding-evolution + claude-md-decision-guide + workflow-selection-guide, index Analysis 섹션 등록
## [2026-06-23] verify | 웹 교차검증 → entities/andrej-karpathy(가이드라인 저자=Forrest Chang, vibe coding 확인), boris-cherny(Vercel CEO 오표기 정정, Claude Code 창시자 확인), mitchell-hashimoto(용어 대중화·명명은 OpenAI), peter-steinberger(PSPDFKit 창업자), google-gemma(Gemma 4 실재·최신 확인), concepts/harness-engineering($9vs$200·OpenAI·LangChain 수치 확인) 외부 출처 각주 추가
## [2026-06-28] ingest | 생각등대 '이력서에 AI 활용 경험 한 줄 제대로 쓰는 법' (youtube-wa6ZoLlnB60, 재생목록 PLUGinkN1Rwv5E7c... #4) — raw/youtube-wa6ZoLlnB60.md 저장(자막 ko) → sources/youtube-wa6ZoLlnB60 + 신규 concepts/ai-resume-writing 생성. 강화: verification-automation(리뷰어=검증자 원리), subagents-agent-teams(리서처/플래너/리뷰어 역할분리), hooks(로깅 훅=재연성·추적성 새 용례), developer-role-change(이력서 변별 축 구현→설계). overview(커리어 응용 관점·새 재생목록 메모) + index 갱신. 모순 없음. 첫 커리어/이력서 관점 흡수.
## [2026-06-28] ingest | 근돌 '2026 개발자 취업 전략' (youtube-Vq9UU92R4wY, 재생목록 PLUGinkN1Rwv5E7c... #5) — raw/youtube-Vq9UU92R4wY.md 저장(자막 ko) → sources/youtube-Vq9UU92R4wY + 신규 concepts/developer-job-strategy 생성. 강화: developer-role-change(AI코드 품질판단=기본기, 취업현실로 구체화), agentic-coding(이해 아웃소싱불가 취업버전), ai-resume-writing(숫자증명·상위전략 링크). overview(취업 전략 두 갈래) + index(재생목록2=취업/커리어 모음으로 정정) 갱신. 모순 없음(상호보완). 재생목록2가 단일채널 아닌 여러채널 모음임 확인(#4 생각등대, #5 근돌).
## [2026-06-28] ingest | 생각등대 '물경력을 이직 가능한 경력으로 바꾸는 3가지' (youtube-SsHGSf_nzrI) — raw/youtube-SsHGSf_nzrI.md 저장(자막 ko) → sources/youtube-SsHGSf_nzrI + 신규 concepts/career-pivot-strategy(물경력 탈출/이직 경력전환) 생성. 강화: ai-resume-writing(동일 로깅훅 이력서 문구 글자그대로 재등장=같은 채널 일관 프레임, 실무 AI흔적 실천법), developer-job-strategy(서비스 운영 디테일+부하테스트/TPS로 ④기둥 구체화), developer-role-change. overview(취업 전략 셋째 갈래=경력자 이직) + index 갱신. 모순 없음. 부하테스트·TPS·도커 단일파드 추정 등 신규 실무 기법 추가.
## [2026-06-28] lint | raw↔sources 28:28 일치·깨진링크 0·고아 0·인덱스 미등록 0. 흡수 중 본문 수정됐으나 updated 미갱신 5건(agentic-coding, developer-role-change, hooks, subagents-agent-teams, verification-automation) → 2026-06-28로 정정. 상호참조 보강 1건(developer-role-change→career-pivot-strategy). 모순/낡은주장 없음.
## [2026-06-28] query | 3년차 DevOps '물경력' 이직 준비 — career-pivot-strategy 프레임 + DevOps 특화 웹리서치(4각도)·다중에이전트 적대적검증(29 에이전트, 검증통과 13/기각 9) 종합 → analysis/devops-career-pivot 로 보존. career-pivot-strategy·developer-job-strategy·index Analysis 등록.
## [2026-07-12] lint | 스캐폴딩 정비(karpathy gist 대비 분석 후속) — 스킬 4종 WORKDIR 경로 오류 수정(llm-wiki→ai-llm-wiki), AGENTS.md 갱신(디렉토리 트리 교정·§6 도메인 확정·새페이지vs수정 휴리스틱·as-of 표기 규칙·확장 기준), 결정적 lint 스크립트 scripts/lint_wiki.py 신설(wiki-lint 1단계로 편입), 소스 페이지 템플릿 파일화, raw 헤더 표준 확정(배치B 형식). 소스 페이지 25건 채널 보강(yt-dlp 확인: 코드팩토리6·짐코딩3·메이커에반3·Jay Choi3·실밸개발자2·castlestudio2 외 — 기존 '미상' 대체, raw 불변 유지). 고아 분석페이지 2건(claude-md-decision-guide, workflow-selection-guide) → concepts/claude-md·dynamic-workflow에서 링크해 해소. git 저장소 초기화(감사 추적·삭제 복구 확보). 발견: 재생목록1에 미흡수 신규 영상 5편(#26~30) 존재.
## [2026-07-12] docs | CLAUDE.md 분할 — 공통 규칙만 CLAUDE.md에 유지, 페이지 작성·인덱스·도메인 규칙은 docs/rules/wiki-content.md, 코딩 규율·사이트 퍼블리싱은 docs/rules/site-code.md로 분리. docs/index.md 목차 신설, 스킬 6종·에이전트 3종·소스템플릿 참조 경로 갱신, AGENTS.md 삭제 반영.
## [2026-07-12] lint | 소스 페이지 5건 출처 raw 경로 절대→상대 정규화 — 발행 시 로컬 사용자명 노출 차단 (site phase 중 evaluator 발견, 메인 세션이 콘텐츠 모드로 병행 수정, 커밋 67ce92e)
## [2026-07-12] site | phase-1-quartz-publishing — Quartz 5 벤더링(site/)·content 상대 심링크·ko-KR+Noto Sans KR 설정·crawl-links/footer 플러그인+lock·gitignore 확인, verify_site.py 신설(9개 검사). T03~T07 전부 Evaluator 검증 통과, 통합 게이트 클린. 로컬 feature/quartz-site 브랜치(원격 없음·미푸시). 남은 수동: T02 비공개 리포·T08 로컬 육안·T09 Cloudflare·T10 프로덕션 확인
## [2026-07-12] delete | 커리어 관련 위키 career-llm-wiki로 이관 — wiki 7개(개념 3·출처 3·분석 1)+raw 3건 삭제, 잔여 링크 7개 파일 정리(index·overview·개념 5), 도메인 경계 규칙 추가
## [2026-07-12] lint | 가독성 개편(콘텐츠) — 본문 위키링크 1,151건에 한글 별칭 일괄 삽입(소스는 frontmatter label 신설: "#N 짧은라벨" 25건), 소스 페이지 자기 인용 164건 제거, 다중 인용 구분자 `, →`→`·` 80건 압축, 조사 12건 받침 교정, index.md 라벨-설명 중복 정리(+`$9 vs $200` KaTeX 위험 표기 수정). 규칙화: wiki-content.md §1(별칭 필수·인용 라벨 형식·자기 인용 금지)+소스 템플릿 동기화. lint_wiki.py 통과.
## [2026-07-12] site | 가독성 개편(코드) — custom.scss 한국어 조판(word-break: keep-all, 본문 line-height 1.75), note-properties 패널 숨김(hidePropertiesView: true — enabled:false로 끄면 frontmatter 파싱 자체가 죽는 함정 발견, site-code.md §2.4 기록). quartz build·verify_site.py 통과, 라이트/다크 육안 확인.
## [2026-07-12] lint | 가독성 포맷 스캐폴딩 마감 — wiki-query·wiki-ingest SKILL.md의 옛 인용 형식 예시를 라벨 방식으로 교체, lint_wiki.py에 링크 형식 검사 3종 추가(본문 별칭 누락·소스 자기 인용·label 키 누락, 위반 주입 테스트로 검증). 이후 흡수/질의는 규칙 문서+템플릿+스킬+결정적 lint 4중으로 새 포맷 강제.
## [2026-07-13] site | phase-2-harness-efficiency — 운영 파일 드리프트·중복 정리 + 결정적 헬퍼 추가. 에이전트 정의 2건의 낡은 raw-leak grep을 verify_site.py 기준으로 교체(T01·T02), 검증 매트릭스·PRD 스키마 중복 제거(T03·T04), 추출 레퍼런스 병합·삭제(T05·T06), wiki 스킬 4종 WORKDIR 블록 제거+ai-llm-wiki 명칭 수정(T07~T09·T11·T13), lint_wiki.py --inbound 플래그 신설(T10), settings.json 권한 allowlist(T12). 13개 태스크 전부 Evaluator 1차 통과, 통합 게이트(lint·build·verify_site) 클린. feature/phase-2-harness-efficiency 브랜치.
## [2026-07-13] site | phase-3-wiki-site-quality — 위키 품질 게이트 강화 + 폰트·CI 정비. lint_wiki.py에 검사 4종 신설: label 형식 검증(불량 25건 검출, T01), 본문 맨 '#숫자' 인용 검출(29건 — Quartz가 태그로 파싱해 쓰레기 태그 페이지 생성, T02), sources·concepts 한글 제목 강제(4건, entities는 면제, T03), 태그 위생 리포트(경고 전용·종료코드 무영향 — 충돌 6쌍·싱글톤 97개, T04). wiki-content.md에 콘텐츠 규약 4종 기록(태그 통제 어휘+병합표·인용 형식·updated 갱신 기준·제목 언어, T05). quartz.config.yaml 폰트 수정(한글 글리프 없는 Schibsted Grotesk/Source Sans Pro 요청 제거, Noto Sans KR만 로드, T06). CI 워크플로 신설(main push 시 빌드+raw/ 경계 감사 게이트, T07). 7개 태스크 전부 Evaluator 독립 검증(fixture 기반) 통과, 통합 게이트 클린(build exit 0, verify_site 9검사 PASS). T08(Cloudflare 연결)·T09(라이브 검증)는 사람 계정 필요 — 미완. feature/phase-3-wiki-site-quality 브랜치.
## [2026-07-13] site | phase-4-namu-test-site — 나무위키형 테스트 사이트(site-test/) 신설. site/ 를 아티팩트·content 심링크 제외하고 복제(T01), content/ 를 wiki/ 로 향하는 상대 심링크 디렉터리로 구성(T02, git mode 120000 확인), 사이트 소유 한국어 랜딩 페이지 작성(T03 — AI 사서 정체성·자동생성 자막 정직성 고지·"처음 오셨나요" 진입 링크 5종·지식 파이프라인). 폴더 내비게이션 제거: explorer·breadcrumbs off(T04), tag-list 칩 on(T05), recent-notes 우측 사이드바 "최근 변경" on(T06, limit 8·linkToMore catalog), baseUrl 을 ai-llm-wiki-test.pages.dev 로 분리(T07), 플러그인 install + lock 동기화(T08). verify_site.py 를 감사 대상 디렉터리 인자로 파라미터화(T09 — 비기본 디렉터리에선 content-root 심링크 검사를 명시적 SKIP). 루트 .gitignore 에 site-test 아티팩트 3종 명시(T10 — 벤더 .gitignore 는 자기 자신을 무시해 클린 클론에 부재). 빌드 exit 0, 렌더 226개 HTML 검사 통과: explorer/breadcrumb 마크업 0건·태그 칩 구조적 확인·최근 변경 60개 페이지·wiki 59개 전 페이지 대응·루트=랜딩(카탈로그 아님)·verify_site.py site-test exit 0(T11). 11개 코드 태스크 전부 Evaluator 독립 검증 통과, 통합 게이트 리드 재확인. T12(Cloudflare 테스트 프로젝트 배포)·T13(휴먼 QA)은 사람 손 필요 — 미완. site/ 는 전 과정 무변경. feature/phase-4-namu-test-site 브랜치.
## [2026-07-17] site | 저장소 명칭 통일 ai-llm-wiki → llm-wiki. 전수조사 4개 범주 24건. 변경 3종: GitHub 저장소 rename(omottomo/ai-llm-wiki → omottomo/llm-wiki, remote 자동 갱신·private 유지 확인), pageTitle "AI·LLM Wiki" → "LLM Wiki"(양쪽 config), baseUrl → omottomo-llm-wiki.pages.dev / omottomo-llm-wiki-test.pages.dev. 과거 기록 21건은 사용자 결정으로 보존 — log.md 3건·완료 페이즈 plan.md·prd.json 18건은 당시 사실의 기록이자 검증 완료된 acceptance 문구(특히 log.md 13행은 'llm-wiki→ai-llm-wiki 수정'이라는 과거 작업 자체가 내용이라 치환 시 자기모순). **배포 이름이 저장소 이름과 다른 이유**: llm-wiki.pages.dev 는 이미 타 조직(almaajo, Cloudflare Access 로 잠김)이 점유 — *.pages.dev 서브도메인은 전역 유일이라 사용 불가. omottomo-llm-wiki/-test 는 NXDOMAIN 확인 후 채택(대조군으로 검사기 작동 확인). 검증: 양 사이트 build exit 0, verify_site.py(기본·site-test) 전 검사 PASS, 빌드 출력 내 옛/오배치 문자열 0건 — 0건마다 양성 대조 병행(226/229건 검출, 렌더 page-title 실제 "LLM Wiki", sitemap loc 실제 omottomo-llm-wiki.pages.dev). chore/rename-to-llm-wiki 브랜치 → PR #6 으로 main 에 rebase 머지.
## [2026-07-17] site | 정정 | 이전 세션들이 남긴 "Cloudflare 연결 미완"(phase-1 T09·T10, phase-3 T08, phase-4 T12)은 여전히 사실 — ai-llm-wiki.pages.dev 는 NXDOMAIN 으로 **한 번도 배포된 적이 없음**. 즉 이 저장소의 사이트는 아직 로컬 빌드만 존재하며 웹에 공개된 적이 없다. baseUrl 은 현재 전부 '앞으로 만들 프로젝트명'을 가리키는 예정값이고, site-code.md §2.2 의 baseUrl↔프로젝트명 일치 규칙은 Pages 프로젝트를 실제로 만드는 시점에 비로소 구속력이 생긴다. 남은 사람 작업: Cloudflare 계정에서 Pages 프로젝트 omottomo-llm-wiki 생성(프로덕션 브랜치 main·루트 site·빌드 'npm ci && npx quartz plugin install && npx quartz build'·출력 public·NODE_VERSION=22) — 상세는 docs/tasks/phase-1-quartz-publishing/plan.md.
## [2026-07-17] site | 빨간 main 복구 — CI 게이트가 신설 이래 2회 실행 2회 전부 실패였음을 발견(phase-3 머지·phase-4 머지). 원인은 site-code.md §2.4 가 이미 "live follow-up" 으로 지목해둔 미해결 구멍: 벤더링된 site/.gitignore 는 2번째 줄이 `.gitignore` 라 자기 자신을 무시 → untracked → 클린 클론에 부재. 로컬은 그 파일이 먼저 매치돼 통과하지만 CI 는 클린 클론이라 site/node_modules·public·.quartz 3건이 FAIL. 루트 .gitignore 에 site/ 3종 추가(site-test 와 동일 형식, 후행 슬래시 없음). 검증은 §2.4 가 지정한 방식대로 git clone --local 로 CI 조건 재현: site/.gitignore 부재 확인 → check-ignore 매치 소스가 루트 .gitignore 11~13행 → npm ci·build·verify_site.py 9검사 전원 PASS. 종료코드는 zsh PIPESTATUS 함정(§2.4)에 걸려 빈 값이 나와 bare 실행으로 재확인, 음성 대조까지 수행(수정 있음 exit 0 / 되돌림 exit 1 / 복원 exit 0). §2.4 갱신: 구멍 해소 반영 + "감시하지 않는 게이트는 게이트가 아니다·빨간 게이트는 없는 것보다 나쁘다" 규칙 신설. fix/site-artifact-ignore 브랜치.
## [2026-07-17] site | CI 게이트 커버리지 구멍 2종 정리 — (a) phase-4 가 만든 site-test/ 가 CI 사각지대였음: 트리거 paths 에도 없고, 감사 스텝도 인자 없는 verify_site.py(=site 만) 였음. matrix(site·site-test)로 두 사이트를 각각 빌드·감사하도록 변경, fail-fast: false 로 한쪽 실패가 다른 쪽 결과를 가리지 않게 함. (b) 감사가 검사하는 대상이 트리거에 빠져 있었음: 루트 .gitignore 변경은 verify_site.py 의 아티팩트 무시 검사를 뒤집을 수 있는데 paths 에 없어 게이트가 안 돌았고(=fix/site-artifact-ignore 를 머지해도 CI 미실행), 워크플로 자신을 고쳐도 재실행되지 않았음 → 둘 다 paths 에 추가. 신설 규칙("빨간 채로 두지 마라·첫 실행 초록불 확인") 준수해 CI 반영 전 클린 클론에서 site-test 선검증: 심링크 mode 120000 보존·아티팩트 3종 루트 .gitignore 매치·npm ci/build exit 0·verify_site.py site-test bare exit 0(9검사, content-root 검사는 설계대로 SKIP). YAML 파싱 검증 완료. fix/ci-coverage-gaps 브랜치.
## [2026-07-17] lint | 콘텐츠 부채 청산 — lint_wiki.py 저장소 사상 첫 exit 0 달성(위반 39 → 0). ① 태그 충돌 6쌍 정규화(18페이지, §4.1 병합표대로 하네스→하네스엔지니어링·ClaudeCode/클로드봇→클로드코드·claude-md/CLAUDE-md→클로드md·Codex→코덱스·엣지AI→온디바이스AI): 표에 없던 MCP/mcp 는 새 쌍이라 canonical=MCP 로 §4.1 에 행 추가(통용 한글형 없는 두문자어·개념 페이지 제목도 MCP; 대소문자 변형은 lint 가 정규화로 잡으므로 KNOWN_TAG_VARIANT_GROUPS 에는 미추가). ② 맨 '#숫자' 인용 61건 제거 — 단일 패턴이 아니라 5종이었음: 번호+자기링크 중복(12)·별칭 패스 잔재 ]](#N)(13)·자기 재생목록 순번(21, 자기 인용 금지라 링크 불가 → # 만 제거·형식 3종을 '순번 N' 으로 통일)·진짜 인용(위키링크+조사)·번호 나열. 조사는 번호가 아니라 라벨 종결 기준(#14 '…100번' → 과), 표는 파이프 이스케이프. 렌더 결과 쓰레기 태그 페이지 24개(/tags/6·/tags/11 등) → 0. ③ 한글 없는 제목 4건: 소스 3건은 영어 원제 미번역이라 기존 관례(raw 원제의 한글화)대로 번역, concepts/mcp 는 '영문(영문)' 이라 관례(라틴+한글 부연)에 맞춰 MCP (모델 컨텍스트 프로토콜) — 본문 H1 동반 수정, §1 별칭 축약 규칙상 기존 [[concepts/mcp|MCP]] 링크는 무영향. ④ updated 21건 원복: 링크·인용괄호 전부 제거한 순수 산문 비교 결과 4건만 달랐고 그마저 조사 교정 → 내용 변경 0건 확정, §4.3 대로 pre-pass 값 복원(recent-notes 가 frontmatter 날짜로 정렬해 가짜 bump 가 진짜 변경을 밀어내고 있었음). §4.1 에 MCP 행·§4.3 에 소급 적용 판단과 '별칭 diff 는 내용 diff 로 보인다' 함정 기록. 일괄 정규식이 스스로 결함 4건(인접 동일 링크·번호+인용 중복)을 만들었고 diff 전량 검토로 적발 — 애초 \" 라벨 사태와 같은 원인. 검증: lint·build·verify_site 전부 exit 0. docs/wiki-lint-pass 브랜치.
## [2026-07-17] site | CI 에 lint 게이트 추가 — §2.4 가 건 조건("lint 가 초록이 된 뒤에만 추가하라")이 같은 날 콘텐츠 패스로 충족되어 반영. verify(site·site-test) 매트릭스와 별개 잡으로 분리(lint 는 wiki/ 만 읽어 Node·npm ci·빌드가 불필요 — 매트릭스에 넣으면 빌드를 기다린 뒤 같은 파일을 사이트 수만큼 중복 검사). 트리거는 기존 paths(wiki/**·scripts/**)로 이미 충족. 신설 규칙("빨간 채로 두지 마라·첫 실행 초록불 확인") 준수해 클린 클론 선검증: bare exit 0, raw↔sources 25:25 정합, 위반 주입 시 exit 1(게이트가 실제로 작동함을 음성 대조로 확인). §2.4 의 "CI 는 의도적으로 lint 를 제외한다" 규칙을 폐기하고 대체 — 이제 lint 위반은 main 을 깨뜨리므로 위키 산문 push 전 lint 실행이 필수(권고 아님). ci/add-lint-gate 브랜치.
## [2026-07-18] ingest | IBM '코드형 인프라(IaC)란 무엇인가' — sources/ibm-infrastructure-as-code + concepts/infrastructure-as-code + entities/terraform·hashicorp 생성, entities/mitchell-hashimoto + concepts/harness-engineering·verification-automation + index 갱신
## [2026-07-19] ingest | Terraform 공식 문서(코어·언어·CLI 발췌 19페이지) — sources/hashicorp-terraform-docs + entities/terraform(대폭 확장)·armon-dadgar·sentinel 생성, entities/hashicorp + concepts/infrastructure-as-code·verification-automation·harness-engineering + index 갱신. 사용자와 범위 합의("코어 + 언어/CLI 기초") 후 Introduction 8 + Language 6 + CLI 5페이지만 추출. 프레이밍 차이 기록: IBM 4단계(쓰기→버전→프로비저닝→배포) vs 공식 3단계(Write→Plan→Apply, Plan을 독립 검토 단계로 격상) — 모순 아닌 강조점 차이라 둘 다 보존. plan/apply 분리·PR speculative plan 리뷰를 검증자동화의 인프라 선례로, Sentinel 정책 강제(Govern)를 훅식 강제의 선례로 연결.
## [2026-07-19] lint | 판단 검사 — 모순 1건·낡은주장(as-of 무마커) 다수·누락페이지 1·태그 시맨틱중복 처리. 결정적 linter 는 통과 상태였고(고아·패리티·형식 0), 아래는 스크립트가 못 잡는 판단 항목: ①모순: 카파시 65줄 CLAUDE.md 스타 수 skills.md '15만+' vs 타 페이지 '10만+' 미화해 → 사용자 결정대로 '잡음 처리(기준일 병기)': skills.md 15만+ 에 자막 편차 주석 달고 andrej-karpathy 외부검증(단일 9만/미러 합산 22만) 상호참조. ②낡은 주장: 명시 as-of 마커는 전부 2026-07 로 신선했고, 문제는 마커 아예 없는 시간민감 주장 → 사용자 결정대로 '소스 시점 마커 일괄': multi-model(토큰단가 1.5→0.48달러·커맨드6개·무한루프버그 → 2026-03 자막 기준), skills(설치19만·설치수 → 2026-06 자막 기준, '올해 1월'→'2026년 1월'), on-device('새로 나온 Gemma 4'→'2026-03-31 출시, 영상 시점 최신'). 3개 페이지 updated 07-19 bump. ③누락 페이지: OPA 4개 페이지가 'Sentinel·OPA' 쌍으로 언급하나 페이지 없어 비대칭 → 사용자 결정대로 'OPA 스타만 생성'(OpenTofu 보류), sentinel·terraform·index 에서 연결. 소스(#27) 뒷받침 범위만 기록하고 Rego·CNCF 등은 미기재. ④태그: 하드충돌 0이나 시맨틱 근접중복 → 사용자 결정대로 '전부 병합' 9군(개발자역할변화←역할변화·직업변화·개발자역할 / 비용절감←API비용·API비용절감·토큰비용 / 기초개념←기초·기초역량 / 진화서사←진화 / 적대적리뷰←적대적검증 / 워크플로우←워크플로우최적화 / 조직←기업 / 자동화←AI자동화 / 컨텍스트엔지니어링←컨텍스트), 15파일 tags 라인만 재작성(§4.3 대로 updated 미bump), 카파시vs카파시가이드라인 등은 별개 referent 라 유지. §4.1 병합표에 시맨틱 병합 9행+비병합 근거 기록(linter KNOWN_TAG_VARIANT_GROUPS 반영은 code-mode 후속). ⑤자동 처리 상호링크 8건(hooks↔sentinel·verification-automation↔sentinel·infrastructure-as-code↔sentinel·mitchell→armon·소스→hashicorp·terraform→armon·opa 연결). 태그 141→127종. 검증: lint exit 0(형식위반 주입분 2건 '#22·#27' 맨해시도 즉시 교정).
## [2026-07-19] site   | 리디자인 — Slate & Signal 팔레트(단일 코발트 액센트 #3b5bdb/#6b8afd) + Pretendard 본문·헤더 적용. quartz.config.yaml 색상 토큰(라이트·다크 9종) 교체 + fontOrigin: local; custom.scss 에 Pretendard/IBM Plex Mono @font-face(jsDelivr /npm/) + --bodyFont/--headerFont/--codeFont 변수. theme.typography 는 Noto Sans KR 유지(빌드시 og-image satori 가 Google Fonts 에서 폰트를 가져오므로 Google-resolvable 필수; 실제 표시 폰트는 custom.scss 가 담당). 콘텐츠·구조 무변경. 검증: npx quartz build exit 0, verify_site.py exit 0, 브라우저 라이트·다크 육안 확인(Pretendard 로드·팔레트 적용 정상).
## [2026-07-19] site   | 리디자인 폴리시 — Slate & Signal 위에 ①여백 리듬(article 제목 상단 여백↑·하단↓, 문단/리스트 간격 정리) ②코드블록 서피스 배경·패딩·8px 라운드 + 인라인코드 헤어라인 칩 ③콜아웃 좌측 액센트 바·라운드·여백, 기본 note 를 --secondary 로 온브랜드화(타입별 시맨틱 색은 유지) ④액센트 톤 미세조정(라이트 #3b5bdb→#2f57cc, 다크 #6b8afd→#6f9bf9, highlight rgba 동기화). custom.scss 오버라이드 + quartz.config 색상. 검증: npx quartz build exit 0, verify_site.py exit 0, 브라우저 라이트·다크 육안 확인. 주: 위키에 Obsidian 콜아웃·코드펜스 사용 페이지 0개라 콜아웃/코드블록 스타일은 적용됐으나 현재 콘텐츠엔 미노출(인라인코드·여백·액센트는 노출됨).
## [2026-07-19] lint | 범위 서술 전수조사 — "이 위키는 유튜브 재생목록 25편 정리물"이라는 낡은 정체성 서술 교정. 배경: overview 도입부는 2026-06-23 초기 구축(재생목록 일괄 흡수) 시점 서술이 재생목록 외 웹 문서 흡수(07-18~) 이후에도 방치된 것. 처리: ①overview 도입부를 '카파시 LLM 위키 실험(gist)에서 착안, 운영자가 고른 자료를 계속 흡수하며 성장하는 지식 베이스(재생목록은 초기 시드)'로 재작성, 진화 서사 문두를 '초기 재생목록 25편'으로 한정, 생태계 지도에 IaC 계보 1줄 추가(소스 #26·#27 인용, frontmatter sources 반영), 출처 범위 메모를 1~25번/26번~ 구분으로 갱신 ②index Sources 헤더 '(재생목록 순서)'→'(흡수 순서 — 1~25번 재생목록, 26번부터 재생목록 외)' ③재발 방지로 규칙 모듈 §3에 Wiki charter(재생목록 정리물로 서술 금지)와 웹 문서 출처 유형 추가. 각 출처 페이지의 '재생목록:' 메타데이터·재생목록 한정 서술("재생목록 내 유일한" 등)은 사실이라 유지. 검증: lint_wiki.py exit 0.
## [2026-07-19] site   | 표 디자인 리디자인 — 위키의 밀도 높은 다열 비교표(2~5열, 셀마다 한국어 산문)가 Quartz 순정 스타일이라 답답·평면적이던 것을 개선. `.page article .table-container`/table 오버라이드(custom.scss, base.scss 위 언레이어드): 라운드 헤어라인 보더 컨테이너(8px)·슬레이트 틴트 헤더(--secondary 9%)·헤더 하단 슬레이트 보더·세로 패딩↑(0.65rem)·상단 정렬(vertical-align:top, 텍스트 양 다른 셀 정렬)·행별 헤어라인 제거 후 지브라 밴드(짝수행 --lightgray 26%)·행 호버·첫 열 세미볼드(비교표 행 레이블). 전 색상 테마 토큰이라 라이트·다크 자동 대응. site 와 site-test 양쪽 custom.scss 동일 반영. 검증: 양쪽 npx quartz build exit 0, verify_site.py exit 0, 브라우저에서 3열(claude-md-decision-guide)·5열(workflow-selection-guide) 표를 라이트·다크 육안 확인(헤더 구분·지브라·상단정렬·5열 가로스크롤 정상). 콘텐츠·구조·설정 무변경.
## [2026-07-19] site   | phase-5 가이드 홈 리디자인(site-test) — 첫 방문자용 카드형 대문으로 재구성(우선순위 직관·단순·가독). ①quartz.config.yaml 내비 플러그인: graph enabled:false(우측 그래프 뷰 제거), explorer enabled:true(좌측), breadcrumbs enabled:true(condition:not-index 유지). enabled 플립만이라 플러그인 재설치·lockfile 변경 없음. ②content/index.md(사이트 소유 정규 파일) 카드형 재작성, 기존 한국어 카피 재사용: 히어로(피치+자막 자동생성 주의 1줄) / '처음이면 — 순서대로' 경로 카드 3(①overview ②analysis/ai-coding-evolution ③catalog) / '분야별로 둘러보기' 카테고리 카드 4(개념18·인물17·출처27·분석3, 각 폴더 페이지 /concepts·/entities·/sources·/analysis 링크, 카운트 수기) / '지름길' 분석 가이드 3(ai-coding-evolution·claude-md-decision-guide·workflow-selection-guide 필 로우) / '지식은 이렇게 쌓입니다' 5단계→4단계 축약. 본문 중복 H1(# LLM 위키) 제거하고 article-title 이 제목 담당. ③custom.scss 에 .home-* 스타일 추가(가산·기존 표/콜아웃 서피스 언어 재사용: 헤어라인 1px+8px 라운드, --secondary 액센트, CSS 카운터 번호 배지, 카운트 필, 지름길 필 로우, 모바일 max-width:600px 1열 붕괴). 테마 토큰만 사용, 아티클 스타일 무변경. ④스파이크(선행): raw HTML div 클래스가 빌드 생존 확인. div 내부 위키링크는 앞뒤 빈 줄로 분리해야 해석됨(CommonMark HTML 블록), 같은 줄 인라인은 리터럴로 남음 → 빈 줄 패턴 채택. 폴더 링크는 trailing-slash 위키링크 `[[concepts/]]` 라야 /concepts/(slug concepts/index) 로 정확히 해석(`[[concepts]]` 는 trailing-slash 없는 ./concepts). 검증: npx quartz build exit 0, verify_site.py site-test exit 0(리터럴 '[[' 잔존 0 = 전 카드 링크 해석 확인), graph 마크업 HTML 0건, explorer/breadcrumbs 아티클 페이지(overview) 노출·index 는 not-index 로 breadcrumb 0, index.html home-* 클래스·카운트 span·폴더 href 렌더 확인, wiki/ 무변경. 미검증(육안 필요): 라이트·다크 시각 패스, 모바일 1열 붕괴, 카드 내 한국어 줄바꿈. 범위: site-test 만 — site(프로덕션)는 시각 검증 후 별도 포팅.
## [2026-07-19] docs   | phase-6-nvk-schema-docs — nvk/llm-wiki 벤치마킹 채택안 A부 실행. 배경: 다른 개발자의 llm-wiki(v0.16.0, 멀티런타임 플러그인 제품)를 조사해 개인 단일주제 위키 규모에 맞는 아이디어만 골라 채택(전체 비교·채택/기각 근거는 docs/benchmarking-nvk-llm-wiki.md). 처리: ①docs/rules/wiki-content.md §1에 credibility(sources 필수 enum high|medium|low + 판정 루브릭)·aliases(concepts·entities 선택 리스트, 한/영 검색 브리지) 규칙 추가 ②lint_wiki.py frontmatter 검사에 credibility enum·aliases 리스트형 검증 추가(stdlib 유지) ③소스 템플릿·wiki-ingest에 credibility 판정 단계 반영 ④소스 27페이지 credibility 마이그레이션(frontmatter만, updated 무변경): 공식문서 2건=high, 투기·의견성 익명 영상 2건(#15·#16)=low, 나머지 교차검증되는 기술 해설 23건=medium. 루브릭(미확인 출처는 '비교차검증·투기적'일 때만 low)을 따라 PRD 약식표기('미확인=low')보다 세분 ⑤wiki-query에 질의 3단계(quick/standard/deep) + 모순 시 credibility 가중 규칙 ⑥루트 backlog.md 신설(흡수 후보 대기열, 미발행, 사실근거 아님)·wiki-ingest/query 연결 ⑦CLAUDE.md §1·§3 죽은 my-skills 라우팅(orchestrate/create-prd-json) 정리 → 코드모드=in-session 직접 실행 서술, orchestrate.md 휴면 표기. 검증: lint_wiki.py exit 0, site·site-test 양쪽 npx quartz build exit 0 + verify_site.py exit 0(신규 frontmatter 키가 빌드·raw 경계에 안전). 미검증(육안): 사이트에서 aliases 검색 동작. 후속: phase-7(volatility+refresh·retract·lint 픽스처 테스트).
## [2026-07-19] docs   | phase-7-nvk-skills-tests — nvk 벤치마킹 채택안 B부 실행(신선도·철회·lint 픽스처 테스트). ①wiki-content.md §1에 volatility(sources 필수 enum hot|warm|cold; 고정 스냅샷=cold, 살아있는 공식문서=hot/warm) 규칙 + lint_wiki.py enum 검사(credibility 자매 패턴) + 소스 템플릿 반영 + 소스 27페이지 마이그레이션(frontmatter만·updated 무bump: terraform=hot·ibm=warm·나머지 25=cold) ②wiki-refresh 스킬 신설(hot/warm 소스 재fetch→raw 캡처와 diff→cosmetic/additive/contradictory 분류→사람 확인 전 위키 무편집; raw/ 불변, 물질적 변경은 새 날짜 캡처 raw/<slug>-YYYY-MM-DD.md, 모순은 keep-both). CLAUDE.md §3 라우팅행(최신화/refresh)·wiki-content.md §5 워크플로 ③wiki-delete에 철회(retract) 4번째 범위: --inbound 블라스트반경→인용문단 <!--RETRACTED-SOURCE--> 마커→남은 소스로 재종합→마커 제거, raw/소스페이지 삭제는 명시 확인 시에만(raw opt-in 유지) + lint에 미해결 마커 검사(잔존 시 exit 1) ④tests/fixtures/golden-wiki(최소 정상 위키 lint 0) + defects/(distinct 위반마다 red 픽스처 21종·각 expect.txt) + scripts/test_lint_wiki.py(stdlib assert 러너: golden→0·각 defect→1+부분문자열, 불일치 시 nonzero) + lint_wiki.py --root(기본동작 byte-identical 유지) ⑤verify-site.yml lint 잡에 test_lint_wiki.py 추가 + paths에 tests/** ⑥dry-run: wiki-refresh(terraform 공식문서 — 당일 캡처라 정의문 verbatim 일치→cosmetic/변경없음 분류 후 무편집 중단), retract(golden 사본에서 src-beta 철회 end-to-end: 마커 주입 exit 1 확인→재종합→확인삭제→exit 0, 실위키 무변경). site-code.md §2.4에 '린트 규칙=스키마' 원칙 기록. 검증: lint_wiki.py exit 0(실위키 67p byte-identical), test_lint_wiki.py 22/22 exit 0, 전브랜치 리뷰 Ready-to-merge(Critical·Important 0). 후속: main 병합 후 CI 첫 실행 green 확인(§2.4), phase-6도 같은 브랜치라 함께 병합됨.
## [2026-07-20] site   | phase-8 사이트 확정 — web/를 대표 대시보드로 채택, Quartz 후보 2벌(site/·site-test/) 삭제 후 web/→site/ 개명. ①디렉터리: rm -rf site site-test(벤더링 Quartz 제거) + web/→site/. ②코드 경로: site/build.py(WEB→SITE 상수, docstring web/→site/), site/test_build_site.py(ROOT/"web"→"site", verify_site.py 인자 "web" 제거, 함수명 test_verify_site). ③scripts/verify_site.py 재작성: site-dir 인자·content 심링크 검사·Quartz 아티팩트(public/node_modules/.quartz) 분기 전부 제거 → site/dist 단일 감사로 축소. ④.gitignore: Quartz 아티팩트 6줄 삭제, web/dist/→site/dist/. ⑤.github/workflows/verify-site.yml: Quartz 매트릭스(site·site-test)+verify-web 두 잡을 build.py 단일 verify 잡으로 통합(test_build_site.py+verify_site.py 무인자), paths에서 site-test/**·web/** 제거. ⑥문서: CLAUDE.md §1 트리(site/ 설명 Quartz→build.py 생성기), site-code.md §2 전면 개정(Quartz→build.py/site/dist/Cloudflare, §2.4에 통합 이력 추가+Quartz 규칙군 역사기록으로 강등), docs/index.md(설명·phase-8 행), .claude/settings.json(quartz 권한 2줄→site 스크립트). 검증: python3 site/build.py exit 0(67p), site/test_build_site.py 12/12(pagefind 인덱스 포함) 통과, verify_site.py exit 0(raw 누출 0·site/dist git-ignored), lint_wiki.py·test_lint_wiki.py exit 0. 후속(미처리): wiki-content.md의 Quartz 렌더 동작 규칙 — 특히 aliases 영문검색이 build.py에서 미노출(기능 결정 필요), orchestrate.md(휴면), benchmarking.md 'Quartz 발행' 표현; phase-8 T07(Cloudflare 배포·육안 QA)는 여전히 열림.
## [2026-07-20] site   | 루트 정리(배포 준비) — ①backlog.md·log.md를 docs/ 밑으로 이동(루트에는 CLAUDE.md 등 필수 파일만 유지 방침), 참조 전수 갱신: CLAUDE.md(§1 트리·§2 제목/grep 예시·§3), docs/rules/site-code.md(헤더·content-root 제외 목록)·wiki-content.md(헤더), 위키 스킬 5종(ingest/query/lint/delete/refresh)의 log·backlog 경로, docs/index.md 카탈로그(구조 트리+standalone 표 행 추가). ②.claude/orchestrate.md 삭제 반영(사용자 삭제) — CLAUDE.md §1 트리 라인 제거·§3 히스토리를 '은퇴·삭제됨'으로 개정, site-code.md 헤더의 어댑터 참조 제거(phase-2·6 prd.json의 언급은 이력 기록이라 보존). ③skills-lock.json은 skills.sh CLI 락파일이라 루트 고정(도구 규약) — 이동 대신 .agents/와 함께 .gitignore 처리(로컬 전용). ④tests/는 삭제 안 함: phase-7 lint 픽스처 스위트(golden+defect 21종)로 scripts/test_lint_wiki.py가 실행하고 CI lint 잡에 연결됨 — 제거 시 CI 파손·'검사마다 red 픽스처' 불변식 붕괴. 검증: lint_wiki·test_lint_wiki·site/build.py·site/test_build_site.py·verify_site.py 전부 exit 0.
## [2026-07-21] ingest | Inpa 'DNS 레코드 종류 완벽 정리' — sources/tistory-inpa-dns-records + concepts/dns-records 신설(웹/네트워크 인프라 기초 첫 페이지: A·AAAA·CNAME·PTR·NS·SOA·MX·TXT·SPF·CAA·HINFO·ISDN 레코드, A vs CNAME 트레이드오프, nslookup·Dig·whatsmydns 조회 도구), concepts/infrastructure-as-code 로 사상적 연결, index 갱신. credibility=medium·volatility=cold. 강화·모순 없음(DNS 선행 페이지 부재로 신규 영역).
## [2026-08-02] ingest | 'Terraform HCL 문법 정리'(자체 노트) — sources/terraform-hcl-syntax + concepts/hcl 신설(블록 구조·라벨 개수, 최상위 블록 6종, resource vs data, variable/output, provider alias, 인수 vs 중첩 블록, 메타 인수·lifecycle, 표현식·for expression·heredoc, 파일명 무의미, 참조=실행 순서, init/plan/apply), entities/terraform 의 HCL 절에서 신규 개념 페이지로 링크·상세 이관, index 갱신. credibility=medium(파생 노트지만 #27 공식 문서와 교차확인 가능)·volatility=cold. 모순 없음, 범위 차이 1건 기록: 노트의 '최상위 블록 6종'은 소형 구성의 실제 사용 집합이며 공식 문서가 다루는 module·로컬 값은 빠져 있음.
## [2026-08-02] site   | phase-10-visitor-facing-site A부 — 방문자용 사이트 개편(T01~T15). ①인용 칩: render_citations가 `(→ [[sources/슬러그|#N 라벨]])`를 위첨자 각주 `<a class="cite">N</a>`으로 접음 — 인용만 든 괄호는 래퍼째 제거, 뒤에 산문이 붙으면 괄호·산문 유지, sources/ 아닌 링크는 무손상. lint_wiki.FENCE_RE로 코드 펜스 보호(link_wikilinks의 ponytail 부채도 함께 상환). 출력 전체에 `(→ ` 0건. ②사서 전용 구획 렌더 제외: strip_internal_sections가 `## 기존 위키와의 연결` 섹션 전체와 `## 출처 정보`의 `raw:` 불릿만 걷어냄 — wiki/ 원본은 그대로(wiki-query·wiki-lint가 읽음), 저자·수집일·URL은 그대로 노출. ③전역 검색: 모든 페이지 헤더에 `#header-search`(홈·404의 `#search`와 별도 인스턴스), 결과는 본문을 밀지 않는 절대배치 드로어, 640px 이하에서 자체 행. ④한 줄 요약: extract_summary(source=`## 한 줄 요약` 첫 문장, 그 외=H1 다음 첫 문단 첫 문장, 마크업 제거·100자 말줄임)를 섹션/태그 목록과 최근 갱신에 노출 — frontmatter 스키마 변경 없음. ⑤홈 재작성: 사이트 소유 한 줄 소개(SITE_DESCRIPTION 상수) + 검색 + '처음이면 순서대로' 3단계(/overview/ → /analysis/ai-coding-evolution/ → /index/) + 카테고리 칩 + 최근 5개(요약 포함). ⑥빵부스러기(index·overview 제외) + /tags/ 색인(사용 수 내림차순, 1회 태그는 details로 접음). ⑦공유·색인 메타: SITE_URL=https://omotomo-llm-wiki.com, description·og:*·twitter:card·canonical(og:image는 제외), sitemap.xml(updated→lastmod, 404 제외) + robots.txt. ⑧test_build_site.py에 7개 불변식 추가(19/19 통과). 검증: lint_wiki·test_lint_wiki·build.py·test_build_site.py·verify_site.py 전부 exit 0. 육안 확인(T22)은 사람 몫으로 남김.
## [2026-08-02] docs   | phase-10 B부 — 배경지식 없는 독자용 리드 문단 39쪽 추가(concepts 19·entities 17·analysis 3). H1 바로 아래, 첫 `## ` 앞에 3~4문장 한국어 문단을 넣고 기존 본문은 한 글자도 건드리지 않음. 리드에는 인용 괄호도 위키링크도 없다 — build.py가 첫 문장을 목록·검색결과·og:description으로 끌어 쓰므로 홀로 읽혀야 하기 때문. sources/는 `## 한 줄 요약`이 그 역할이라 제외. 각 파일 updated=2026-08-02로 갱신. 규칙 기록: wiki-content.md §1에 리드 문단 요건 + 사서 전용 구획(기록은 하되 발행 안 함) 표 + §4.2에 인용 형식이 렌더러가 파싱하는 load-bearing 규약임을 명시, site-code.md §2.4에 렌더 타임 필터와 인용 파서-규약 결합(한쪽 고치면 다른 쪽도) 기록, docs/index.md에 phase-10 등재. 검증: lint_wiki·test_lint_wiki·build.py exit 0.
## [2026-08-03] site   | phase-11-ci-deploy-gate — verify가 deploy를 실제로 게이트하도록 워크플로 통합(T01~T03). 문제: verify-site.yml과 deploy-site.yml이 둘 다 `push: branches: [main]`이라 GitHub이 동시에 시작했고 서로 기다리지 않았다 — deploy는 S3 sync 직전 test_build_site.py·verify_site.py를 인라인으로 다시 돌려서 게이트된 것처럼 보였을 뿐, lint_wiki.py는 그 인라인 사본에 없어서 **lint 위반이 CI를 빨갛게 만들면서도 사이트는 그대로 배포**됐다. ①두 파일을 .github/workflows/site.yml 하나로 합치고 deploy에 `needs: [verify, lint]` 선언 — 순서의 전부가 이 한 줄. deploy-site.yml 삭제는 같은 커밋(나뉘면 main이 잠깐 배포 경로 2개를 갖고 이중 배포). ②deploy에서 test_build_site.py 제거(내부에서 build.py+pagefind를 또 돌려 push당 3회 빌드 중이었음), verify_site.py는 유지 — verify job은 다른 러너의 다른 dist/를 봤고 이건 퍼블릭 버킷에 올라갈 바로 그 트리를 감사하므로 의도적 중복(§2.1). ③concurrency를 워크플로→deploy job 레벨로(안 그러면 verify/lint까지 직렬화), permissions는 워크플로 contents:read + deploy에만 id-token:write. ④액션/파이썬 통일(checkout@v7·setup-node@v6·setup-python@v6, python-version이 verify "3"/deploy "3.12"로 갈려 검증과 배포가 다른 인터프리터일 수 있었음 → 세 job 모두 "3.12"). 파일명 verify-site.yml→site.yml(프로덕션 S3에 배포하는 파일이라 이름이 실제를 축소했음) — OIDC trust는 repo+ref만 핀하므로(infra/iam-deploy.tf:22-29, job_workflow_ref 없음) 안전, 단 branch protection은 job 이름 기준이라 verify/lint/deploy는 유지. 검증: 로컬 §2.3 매트릭스 5종 전부 exit 0 + git check-ignore site/dist, s3 sync·CloudFront 블록은 원본과 diff 클린. 머지 후 첫 실행(#30792463722) green — deploy.startedAt 07:07:44가 verify 완료 07:07:42(+2s)·lint 완료 07:07:31(+13s)보다 뒤로, 순서가 실제로 강제됨을 타임스탬프로 확인(§2.4: 아무도 안 보는 게이트는 게이트가 아니다). 범위 밖으로 남긴 것: CI는 여전히 PR이 아닌 push to main에서만 뜨므로 깨진 커밋이 main에 들어오는 건 막지 못한다.
## [2026-08-03] site   | phase-12-wiki-identity-copy — 위키 정체성 서술 전수조사(T01~T08). 문제: overview·index·개념/엔티티/분석 페이지와 사이트 카피, 그리고 `docs/rules/wiki-content.md` §3 charter가 이 위키를 ①유튜브 재생목록 25편 정리물로, ②중심 주제가 Claude Code·하네스 엔지니어링인 위키로 규정하고 있었다. 둘 다 사실이 아니다 — 무엇을 흡수할지는 매체가 아니라 운영자의 선택이 정하고, 위키의 정체성은 고른 지식을 축적·발전시켜 누구나 조회할 수 있게 하는 것이며 AI 에이전트 코딩은 고른 주제 중 하나다. 처리: ①`wiki/overview.md` 제목을 「위키 개요」로 바꾸고 리드를 축적·조회 정체성으로 재작성, 재생목록 시드·자막 안내·"중심 주제"·"중심 개념"·"중심 도구" 문장 제거 ②`wiki/index.md` 머리말의 출처 매체 서술과 Sources 헤딩의 재생목록 기준선 제거, overview 별칭 갱신 ③`concepts/harness-engineering`·`infrastructure-as-code`의 "이 위키의 중심 개념" 4건 제거(리드 3문장 요건 유지) ④`entities/claude-code`·`anthropic`·`mitchell-hashimoto` — 리드 안에 있던 2건은 삭제 시 리드가 2문장으로 줄어 extract_summary가 읽는 첫 문장이 깨지므로 주제 중립 문장으로 교체, 나머지는 삭제 ⑤`analysis/ai-coding-evolution`의 "위키 전체를 관통" 2건 완화 ⑥`site/build.py` SITE_DESCRIPTION을 운영자 확정 문구("직접 고른 자료를 읽고 정리해 쌓아 올리는 개인 지식 위키입니다")로 교체하고 START_PATH 라벨 정렬 ⑦charter를 정체성(축적·조회)과 목록(지금까지 쌓인 주제)으로 분리해 다시 씀 ⑧사서 전용 `## 기존 위키와의 연결` 5줄의 코퍼스 지칭("재생목록 내 유일한" 등)을 위키 기준으로 정정. 보존: 출처 페이지의 `재생목록:`·`자막:`·URL 메타데이터와 개별 주장의 자막 오인식 표기(개별 자료의 사실), 모든 `#N` 라벨(render_citations가 각주 번호를 여기서 뽑음). 검증: lint_wiki.py·test_lint_wiki.py(22/22)·build.py(71쪽)·test_build_site.py·verify_site.py 전부 exit 0, 홈 meta description·intro와 overview `<title>`이 새 문구 확인.
## [2026-08-04] site   | phase-14-readable-pages — concepts·entities·analysis 40쪽 본문 포멧·어투 전면 개정(T01~T15). 문제: 본문 템플릿이 `sources/`에만 있어서 나머지 세 유형은 frontmatter·인용 형식만 공유했다 — 마무리 링크 섹션 이름이 `관련 문서`7·`같이 보기`6·`연결`5로 갈렸고, 코퍼스 전체에 H3가 **0개**라 harness-engineering 11,053자를 H2 10개로만 나눴으며(덩이당 1,100자), 표 5/40·코드블록 1/40·이미지 0/40에 한 문단 5~7문장·문장 120자 초과가 흔했다. 참조 스타일은 사용자가 지정한 inpa.tistory.com — 헤딩 3단계, 첫 등장 즉시 풀이, 비교는 표, 주의는 콜아웃, 설명하는 어투 7가지를 채택하고 스크린샷 밀도는 버렸다. 처리: ①`docs/rules/wiki-content.md` §1.1 유형별 필수 헤딩(concepts 한눈에 요약/함께 읽기, entities +이 위키에서의 등장, analysis 결론 먼저/비교표/함께 읽기), §1.2 검사 가능한 쉬운글 규칙 8개(문장 100자·문단 4문장·H2 1,200자·용어 첫 등장 풀이·3개 이상 비교는 표·주의는 blockquote·핵심어 굵게·비유 한 번), §1.3 어투(종결어미 섞기·쉬운 말 재진술·완충 연결어·걸릴 지점 짚기·정의 전 동기 한 문장; 청유형·수사의문문·여러분·이모지는 미채택) ②템플릿 3종을 `source-page.md` 옆에 추가하고 wiki-ingest SKILL.md가 가리키게 함 ③`lint_wiki.py` `check_page_structure` — 필수 헤딩 누락과 리드 문단 잔존은 오류, 구 헤딩·과대 H2·과대 문장은 종료 코드와 무관한 경고 채널(golden 픽스처 갱신 + 결함 픽스처 2종, 24/24 통과) ④**phase-10 리드 문단 제거** — `## 한눈에 요약`과 같은 말을 두 번 하고 있었다. `extract_summary`가 리드 대신 요약 불릿(analysis는 `## 결론 먼저` 인용문)을 읽도록 `SUMMARY_GLANCE_RE` 추가, 미개정 페이지용 폴백 유지, 불릿 한 줄이 짧아 토막나는 걸 막는 `SUMMARY_MIN=45` ⑤40쪽 개정 — H3 69개 신설, 7열짜리 대형 표 2개를 5열 이하로 줄이고 상세는 H3로 분해, 인용 괄호 505→506(소스 슬러그는 전량 보존) ⑥사이트: `add_toc`/`split_h1`으로 H2·H3 자동 목차(1180px 이상 sticky 사이드 칼럼 + 스크롤 하이라이트, 헤딩 3개 미만은 생략), h1이 `<article>` 밖으로 나가며 Pagefind 제목 색인이 깨지는 걸 `<h1 data-pagefind-body>`로 방어, 백링크 41개짜리 목록을 `<details>`로 접음. 결과: 리드 잔존 0, 구조 오류·가독성 경고 0, 한눈에 요약 37/37·결론 먼저 3/3·함께 읽기 40/40. 검증: lint_wiki.py·test_lint_wiki.py(24/24)·build.py(71쪽)·test_build_site.py(9종, 목차 앵커 정합성 테스트 신설)·verify_site.py 전부 exit 0. 되돌리기 주의: 리드 문단 제거는 2026-08-02 phase-10 결정의 의도적 번복이라 §1 본문 규칙에 명시해 뒀다 — 다음 세션이 "복구"하지 않도록.
## [2026-08-06] site   | overview 재작성 — 축적된 주제 지도(4단계 서사·하네스 골격·도구 지도·긴장점·커리어 이관 메모)를 걷어내고 **이 리포지터리가 굴러가는 방식** 설명으로 교체. 처음 오는 사람에게 필요한 건 도메인 요약이 아니라 "이게 어떻게 만들어지는가"라는 판단(사용자 요청). 새 구성: 한 문장 요약 + 파이프라인 도식(원본→정리 페이지→사이트), 폴더 3개 표, 페이지 4유형 표와 새 페이지 대 기존 페이지 수정 기준, 스킬 5개 표(ingest/query/lint/refresh/delete), ingest 5단계와 강화·모순·신규 3갈래 표, 위키링크·인용·색인으로 연결이 만들어지는 방식, 무너지지 않게 붙잡는 장치(검사 스크립트·lint·refresh·사람 확인 게이트·git)와 "지우지 않는다" 원칙. 리드 문단은 사용자가 지정한 문구를 유지(마지막 한 줄만 지도→설명으로 교체). 어투는 사용자 요청으로 페이지 전체를 **존댓말(~습니다)**로 통일 — `docs/rules/wiki-content.md` §1.3이 규정한 해라체(~이다/~한다)와 다르므로 overview 한 쪽만 예외이며, 나머지 70쪽은 그대로다(전면 통일은 미결), frontmatter sources는 빈 목록(더 이상 특정 자료에 기대지 않음). 부수 수정 2건: `site/build.py` START_PATH의 overview 한 줄 설명("무엇을 다루는지"→"어떻게 만들어지는지"), `site/test_build_site.py`의 백링크 테스트가 overview→harness-engineering 링크를 하드코딩하고 있어 context-engineering→harness-engineering 쌍으로 옮김. 함정 2건: 위키링크 문법을 코드펜스로 예시하면 `verify_site.py`의 "미해석 `[[` 없음" 감사에 걸리고(문법 예시는 산문으로 대체), 폴더를 `raw/`로 적으면 raw 유출 감사의 화이트리스트(`raw: raw/<slug>.md` 형태만 허용)에 걸린다 — 본문에서는 슬래시 없이 `raw` 폴더로 적었다. 이어진 요청으로 처리한 것 4건: ①`docs/rules/wiki-content.md` §1.3에 존댓말 예외 조항 명시(overview 한 쪽만, 다른 페이지로 확산 금지, 원복 금지) ②홈 `START_PATH` 위 헤딩 "처음이십니까?"→"처음이신가요?" ③overview 말미 「여기서부터 읽기」 절 제거 — 그 절의 색인 링크를 가리키던 "이 문서 맨 아래의"를 "바로 이"로 고쳐 자기 참조로 바꿨다 ④「한 문장으로 말하면」의 ASCII 파이프라인 도식을 카드 3장+화살표로 교체(본문 인라인 HTML `div.flow`, CSS 20줄 신설, 560px 이하 세로 스택). 폭 조정: 사용자가 큰 모니터에서 본문이 답답하다고 지적 — 원인은 세 겹의 상한 중 `article p, li { max-width: 66ch }`였다. `ch`는 `0` 글자 폭 기준이라 한글로는 한 줄 33자(약 570px)뿐인데 같은 페이지의 표는 856px를 다 써서 폭이 어긋나 보였다. `main` 62→70rem, `main.has-toc` 74→84rem(본문 칸 856→1080px), 문단 66→80ch(한글 약 40자)로 올렸고 홈(hero·entries·recent)의 74rem은 카드 그리드라 유지했다. 검증: lint_wiki.py·test_lint_wiki.py·build.py(71쪽)·test_build_site.py·verify_site.py 전부 exit 0, 고아 페이지 0(overview 링크가 사라져도 index가 전량 등재).
## [2026-08-06] site   | phase-15-public-repo — 저장소를 포트폴리오용으로 공개(T01~T10). 문제: `omottomo.github.io`의 프로젝트 섹션에 llm-wiki를 넣으려면 남이 읽을 수 있어야 하는데, 저장소 규칙(`docs/rules/site-code.md` §2.1 "must never land in a public repo", "Never flip the repo to public")이 이를 금지했다. 그 규칙은 낡은 게 아니라 `raw/`가 남의 저작물(유튜브 자동 자막 26편·HashiCorp 문서 발췌·IBM 기사·티스토리 글)이라서 있었다 — **규칙이 아니라 규칙의 사유를 제거**하는 방향으로 처리했다. 감사에서 나온 것: `raw/` 29건이 전 커밋에 추적됨, 실명 이메일 2개(`whddlsk123@naver.com`·`whddlsk97@gmail.com`)가 작성자로, AWS 계정 ID `435432369533`이 4개 파일과 히스토리에. 자격증명은 0건(OIDC라 장기 키 자체가 없음). 처리: ①`raw/`를 중첩 비공개 저장소 `omottomo/llm-wiki-raw`로 분리하고 부모는 gitignore ②`lint_wiki.py`가 `raw/` 부재 시 exit 2로 죽던 것을 내용 없는 `docs/raw-manifest.txt` 폴백으로 대체 — 패리티 규칙이 CI에서 살아남는다. 둘 다 있고 어긋나면 오류, `--update-manifest`로 갱신 ③`verify_site.py`에 `git ls-files raw/` 공집합 검사 추가 — 경계의 첫 번째 다리를 "저장소가 private"에서 기계 검사로 교체 ④계정 ID 제거: backend "s3"는 변수 보간이 안 되므로 `bucket`만 부분 구성(`backend.hcl`, gitignore)으로 빼고 `terraform init -backend-config=backend.hcl`이 필수가 됨, tfvars도 gitignore + `.example` 추적 ⑤`.obsidian/`(서드파티 플러그인 번들 포함)과 `.agents/`를 가리키던 심볼릭 링크 13개 untrack, LICENSE(MIT, 코드)와 README 추가, `wiki/` 산문은 CC BY 4.0 — 남의 자료 기반 합성물이라 출처 표시를 요구하는 쪽이 맞다 ⑥규칙 재작성(§2.1·CLAUDE.md 핵심 원칙 1·ingest 2커밋 절차) ⑦`git filter-repo` 1회로 `raw/` 삭제 + mailmap + 계정 ID 치환(`--replace-text`와 `--replace-message` 둘 다 — 스쿼시 머지 커밋 메시지에도 ID가 있었다), `--prune-empty=never`로 커밋 수 115 보존(6개는 `raw/`만 건드려 빈 커밋이 됨), `tests/fixtures/*/raw/` 50건 생존 확인 ⑧기존 저장소를 `llm-wiki-archive`로 rename해 **private 동결**하고 새 public `omottomo/llm-wiki`에 재작성 히스토리를 push. 강제 푸시가 아니라 새 저장소인 이유: `refs/pull/N/head`가 머지된 PR마다 재작성 전 커밋을 고정하고 dangling object도 SHA로 fetch되므로, 공개하는 순간 지운 자막과 이메일이 SHA 하나 거리에 남는다 ⑨룰셋으로 main 보호(verify·lint required, 직접 push 차단) — free plan private에서 403이던 그 기능이다. Actions 변수 3개는 **secret**으로 전환(public 실행 로그는 `vars.*`를 마스킹하지 않아 버킷명에 박힌 계정 ID가 매 배포마다 찍힌다) ⑩포트폴리오 `omottomo.github.io`의 개인 프로젝트 섹션에 항목 추가(사이트·저장소 2링크). 함정 3건: **새 저장소는 repo ID가 달라** OIDC `sub`의 immutable 형식이 어긋나 첫 배포가 `Not authorized to perform sts:AssumeRoleWithWebIdentity`로 죽었다(1298234217→1324905842, `terraform apply` 필요 — 설정 수정만으로는 AWS의 신뢰 정책이 안 바뀐다), untrack 커밋을 pull한 작업 트리는 해당 파일을 **삭제**하므로 `raw/` 29건과 tfvars가 사라졌다(각각 비공개 원격과 미러에서 복구), phase-15 계획서 자신이 감사 결과로 계정 ID를 담고 있어 같이 치환해야 했다. 검증: 공개 URL을 새로 클론해 `raw/` 없음·이메일은 noreply 2종만(개인 주소 0)·계정 ID 0건·끊긴 심볼릭 링크 0 확인, 그 클론에서 lint·test_lint·build·test_build·verify 전부 exit 0(CI가 타는 바로 그 경로), 일회용 PR로 lint red 시 `mergeStateStatus=BLOCKED`와 해당 head SHA에 deploy 미실행 확인 후 머지 없이 닫음.
## [2026-08-09] ingest | sudoremove '하네스/루프/그래프 엔지니어링, 순서대로 이해하기 (with Noto)' — sources/youtube-lokHQ8_b5Rk + concepts/graph-engineering·agent-contract + entities/noam-brown·langgraph·hermes·andrew-ng 생성, concepts/harness-engineering(용어 기원 모순② 표 + 넓은 정의 + 만드는 사람 vs 쓰는 사람)·loop-engineering(정의 2종 병기 + 루프 4종)·context-engineering(롱 호라이즌 배경 + 관리 4방식)·developer-role-change(코드 미열람)·dynamic-workflow(적합 태스크·토큰 300~400달러)·verification-automation(개발이 검증하기 쉬운 이유)·skills(평가 난제 비판)·llm-basics(인컨텍스트 러닝도 학습) + entities/mitchell-hashimoto·boris-cherny + analysis/ai-coding-evolution(5단계 그래프 + 전사) + index 갱신. 핵심 산출: 하네스 용어 기원이 노암 브라운(부정적 뉘앙스)이라는 증언이 기존 하시모토 대중화설과 충돌 — 양쪽 보존해 모순②로 기록.
