---
title: etcd
type: entity
created: 2026-08-11
updated: 2026-08-11
sources: [k3s-docs]
tags: [쿠버네티스, 데이터스토어, 고가용성]
---

# etcd

## 한눈에 요약

- 쿠버네티스 클러스터의 상태가 실제로 저장되는 분산 키-값 저장소다. 여러 대에 같은 데이터를 복제해 한 대가 죽어도 살아남는 구조다.
- 이 위키에는 [[entities/k3s|K3s]]의 고가용성 구성을 설명하는 자리에서 처음 등장한다.
- 홀수 대수로 묶어야 하고, 쓰기가 잦아 느린 디스크에서 병목이 된다는 두 가지가 운영의 핵심이다.

## 정족수는 왜 홀수를 요구하나

여러 대가 같은 데이터를 나눠 갖는 시스템은 "몇 대가 동의해야 결정으로 치나"를 정해야 한다. 그 기준이 **정족수(quorum)**다. 서버 n대라면 (n/2)+1대가 살아 있어야 클러스터가 돌아간다 (→ [[sources/k3s-docs|#32 K3s 공식 문서]]).

여기서 짝수가 손해인 이유가 나온다. 홀수 클러스터에 한 대를 더하면 정족수 기준도 같이 올라간다. 대수는 늘었는데 견딜 수 있는 장애 대수는 그대로여서, 고장 날 수 있는 기계만 늘어난 셈이 된다 (→ [[sources/k3s-docs|#32 K3s 공식 문서]]).

## K3s에서의 자리

K3s는 etcd를 두 가지 방식으로 쓴다. 서버 안에 끼워 넣은 **내장 etcd**(서버 3대 이상)와, 바깥에 따로 세운 **외부 etcd**다. 외부 저장소로는 etcd 3.5.21이 인증돼 있다 (2026-08 기준) (→ [[sources/k3s-docs|#32 K3s 공식 문서]]).

내장 etcd HA에서는 서버끼리 2379·2380 포트가 열려 있어야 한다. 메트릭은 2381, 레거시 HTTP API는 2382 로컬 포트를 쓴다 (→ [[sources/k3s-docs|#32 K3s 공식 문서]]).

> 라즈베리 파이처럼 SD 카드·eMMC로 도는 장비에서는 성능 문제가 난다. etcd가 쓰기 집약적이라 IO를 감당하지 못하기 때문이다. 외장 SSD를 쓰라는 것이 문서의 권고다 (→ [[sources/k3s-docs|#32 K3s 공식 문서]]).

백업은 `k3s etcd-snapshot` 명령으로 뜬다. 스냅샷만으로는 부족하고 서버 토큰까지 같이 보관해야 복원할 수 있다 (→ [[sources/k3s-docs|#32 K3s 공식 문서]]).

## 이 위키에서의 등장

- **HA 데이터스토어** — 서버 3대 이상 구성에서 클러스터 상태를 나눠 갖는 역할 ([[entities/k3s|K3s]])
- **쿠버네티스 기본 저장소** — 배포판이 갈아 끼울 수 있는 자리 중 하나 ([[concepts/kubernetes|쿠버네티스]])

## 함께 읽기

- [[entities/k3s|K3s]] — 데이터스토어를 SQLite·etcd·외부 DB 중에 고르게 한 배포판
- [[concepts/kubernetes|쿠버네티스]] — 이 저장소가 담는 상태가 무엇인지
